bootstrap navbar will go here! for now, name or sign out
<% if logged_in? %>
  Hi, <%= current_user.fname %>!. <%= button_to 'Sign Out', session_url, method: delete %>
<% else %>
  <%= link_to 'Sign In', new_session_url %>
<% end %>
